The mental models behind each milestone: why the design is the way it is, and what failure
mode motivated it. Written by `/m-start N`. One file per topic: `MN-<topic>.md`.

These explain *why*. For *what gets built and in what order*, start at
`docs/walkthroughs/MN.md`, which links here. For a one-line definition of a term, see
`docs/lexicon.md`.
