One file per milestone, written by `/m-start N` before any code exists: what gets built, in
what order, and why. Start here — it links out to the concept notes and the primary docs.

Read the implementation diff against the walkthrough for that milestone. If they disagree,
the walkthrough is wrong and gets fixed at `/m-close`.
